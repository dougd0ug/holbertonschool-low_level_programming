#include "main.h"

/**
 * mul - Entry
 * @a: the digit we want to check
 * @b: the second digit
 * _putchar - writes the character c to stdout
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int mul(int a, int b)
{
	return (a * b);
}